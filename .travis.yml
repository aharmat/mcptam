sudo: required
dist: trusty

language:
    c++

compiler:
    - gcc

before_script:
    - sudo bash scripts/ros_install.bash
    - sudo bash scripts/ci_install.bash

script:
    - sudo bash scripts/run_tests.bash

branches:
    only:
        - cper

notifications:
    recipients:
        - arun.das.uwaterloo@gmail.com
    email:
        on_success: change
        on_failure: always